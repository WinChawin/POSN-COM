#include <iostream>
#include <string>
using namespace std;

int main(){
    string str;
    int start, len;
    cin >> str >> start >> len;    // chawin 0 3
    cout << str.substr(start, len) << endl;   // substr(start_index, length)  // cha
}